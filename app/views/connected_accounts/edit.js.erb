$('#<%= dom_id(@connected_account) %> .tokens').parents().removeClass("small")
$('#<%= dom_id(@connected_account) %> .tokens').parent().append('<%= j(render(partial: "form", locals:{connected_account: @connected_account})) %>');
$('#<%= dom_id(@connected_account) %> .tokens').hide();
